name: pratice

on: 
    branches;:
            pull: ["main"]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
         -name: check out
          uses: actions/checkout@v3
        
         -name: files
          run: ls -la
             
    
                    

