name: pratice

on: 
    branches;:
            pull: ["main"]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
         -name: check out
          uses: actions/checkout@v4
        
         -name: files
          run: ls -la
             
    
                    

