name: pratice

on: 
 push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: chekout
        uses: actions/checkout@v6

      - name: files
        run: ls -la
     
         
    
                    

